<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Diretório de cache interno para arquivos temporários -->
    <cache-path
        name="temp_images"
        path="temp/" />

    <!-- Diretório de arquivos internos para fotos permanentes -->
    <files-path
        name="comprovantes"
        path="comprovantes/" />

    <!-- External files directory (se necessário) -->
    <external-files-path
        name="external_comprovantes"
        path="comprovantes/" />
</paths>
